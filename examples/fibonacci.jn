/*
This returns the n'th Fibonacci number.
*/

fn fib(n: Integer) -> Integer:
  if (n < 2):
      return n
  end
  return fib(n - 2) + fib(n - 1)
end

fn main():
    let iter = 40 # Change this to choose the n'th Fibonacci number.
    echo(fib(iter))
end