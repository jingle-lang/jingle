/*
Returns the factorial of x
*/

fn factorial(x: Integer) -> Integer:
    if (x == 0):
        return 1
    end else:
        return factorial(x - 1) * x
    end
    return 1
end

fn main():
    let num = 5
    echo(factorial(num))
end